# PicSift 使用指南

## 1. 打开工作台

1. 打开支持站点的商品详情页。
2. 等待主图加载；详情图为滚动加载时，先滚动到需要提取的区域。
3. 点击浏览器工具栏中的 PicSift 图标。
4. PicSift 会打开或复用一个全屏工作台标签页。

右上角绿色站点徽标表示已连接来源商品页。点击徽标可以返回来源页。

## 2. 提取图片

### 设置尺寸

- “最小宽度”和“最小高度”用于排除小图标、Logo 和缩略图。
- 输入范围为 `0` 到 `10000` px。
- 默认提取门槛为 `750 × 750`，也可按目标平台要求自定义。

### 设置比例

- 方图：接近 1:1。
- 横图：宽度大于高度。
- 竖图：高度大于宽度。
- 同时选择多个比例时，符合任意一个已选比例即可保留。
- 不选择比例时，不按方向过滤。

### 开始扫描

点击“提取本页图片”。PicSift 会扫描：

- `<img>` 元素及常见懒加载属性；
- CSS `background-image`；
- 页面元数据和已加载图片资源；
- 支持站点的专用商品图选择器；
- Alibaba 页面中包含图片 URL 的结构化数据。

扫描完成后，状态栏会显示符合条件的图片数量。

## 3. 按结果规格即时筛选

图片提取完成后，控制栏会显示“结果规格”：

- 尺寸：全部、750+、800+、1000+、1500+；宽高必须同时达到所选尺寸。
- 比例：全部、方图、横图、竖图。
- 数量：显示“当前结果 / 提取总数”。

结果规格只过滤已经提取的图片，切换时不会重新扫描商品页。被新规格隐藏的图片会从选择中移除，避免误下载。

## 4. 选择与预览

- 单击图片卡片：选择或取消选择。
- 双击图片卡片：打开大图预览。
- “全选”：选择当前结果中的全部项目。
- “取消”：清空当前标签页选择。
- 卡片底部显示检测到的原始尺寸。

## 5. 下载图片

### 单张图片

选择一张图片后点击“下载选中图片”，浏览器会直接保存该文件。

### 多张图片

选择两张及以上图片后，按钮会变为“打包下载 N 张图片”。PicSift 会：

1. 并发获取所选图片；
2. 在浏览器本地生成一个 ZIP；
3. 只触发一次下载；
4. 将失败的图片保留为选中状态，便于重试。

ZIP 限制：

- 单个资源等待上限：20 秒；
- ZIP 内容总上限：256 MB；
- 仅接受 HTTP(S) 图片 URL；
- 图片服务器拒绝跨域访问时，该图片可能无法加入 ZIP。

## 6. 提取视频

1. 切换到“视频”标签。
2. 点击“提取本页视频”。
3. 选择可用视频并下载。

当前仅支持页面可直接访问的 HTTP(S) 视频文件或数据属性中的直链。M3U8、DASH、DRM 和需要临时鉴权的流媒体可能无法下载。

## 7. 输出文件

单个媒体下载使用以下目录规则：

```text
PicSift/<域名_日期>/images/<序号_时间戳>.<扩展名>
PicSift/<域名_日期>/videos/<序号_时间戳>.<扩展名>
```

多图 ZIP 使用站点和日期生成文件名，ZIP 内图片按顺序编号。

## 8. Alibaba 使用提示

- 页面出现登录、安全验证或访问拦截时，先在来源页完成验证，再重新提取。
- 新版页面可能分批加载详情图片，建议滚动页面后再次扫描。
- 如果工作台刷新后来源丢失，PicSift 会尝试恢复最近访问的受支持商品页。
- 右上角站点徽标显示黄色时，返回商品页后再次点击扩展图标。

## 9. 故障排查

### 提取结果为 0

1. 降低最小宽高。
2. 清除比例筛选。
3. 滚动到详情区域，等待图片加载。
4. 检查来源页是否停留在登录或验证码页面。
5. 在扩展管理页重新加载 PicSift。

### 图片很多但显示异常

确认版本不低于 `v0.0.4`。旧版本在大批量结果下可能出现网格重叠。

### ZIP 中缺少部分图片

通常是图片服务器拒绝跨域获取、请求超时或总大小超过限制。状态栏会显示成功和失败数量，失败项仍保持选中，可减少数量后重试。

### 下载按钮不可用

必须先选择至少一个项目。下载过程中按钮会暂时禁用，防止重复提交。

安装问题见 [INSTALL.md](INSTALL.md)，开发者排查见 [TESTING.md](TESTING.md)。
